package com.example.prueba.ui.viewmodel

import androidx.lifecycle.ViewModel

// Esta clase se mantiene vacía para evitar conflictos con imports antiguos.
// La lógica ahora está en ScheduleViewModel.
class StudentViewModel : ViewModel() {
    // Clase vacía intencionalmente.
}